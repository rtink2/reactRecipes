import React from 'react';
import { CircleLoader } from 'react-spinners';

const Spinner = () => (
  <div className='spinner'>
    <CircleLoader color={'#BD10E0'} size={60} />
  </div>
);

export default Spinner;
